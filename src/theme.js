import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#475dff',
    },
    info: {
      main: '#757da2',
    },
  },
});

export default theme;
